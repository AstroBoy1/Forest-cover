I included a requirements.txt file that contains the python packages I used
Otherwise, you just need to include covtype.data/covtype.csv in the project directory to run the notebook
Also, the covtype data shouldn't have headers
I have a file called features which lists the attributes